import 'package:flutter/widgets.dart';

/// Constantes de color para toda la biblioteca
class BrutalConstants {
  /// Color transparente para usar en toda la biblioteca
  static const transparent = Color(0x00000000);

  /// Color rojo para efectos glitch
  static const red = Color(0xFFFF0000);

  /// Color cian para efectos glitch
  static const cyan = Color(0xFF00FFFF);

  /// Color blanco
  static const white = Color(0xFFFFFFFF);

  // Prevenir instanciación
  const BrutalConstants._();
}
